# pgpreflight-core

Stable configuration, normalized evidence, diagnostics, and schema-v1 report types for [`pgpreflight`](https://github.com/tappe9/pgpreflight).

This crate contains no PostgreSQL driver or SQL parser types in its public API.

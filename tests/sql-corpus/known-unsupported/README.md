# Known unsupported SQL corpus

PostgreSQL-valid statements that `sqlparser-rs` cannot currently parse or that pgpreflight cannot yet validate safely belong here.

No concrete fixture is recorded yet. Add one only after reproducing the PostgreSQL-valid / pgpreflight-unsupported behavior, and document the reason alongside the fixture.
